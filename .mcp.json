{
  "mcpServers": {
    "playwright": {
      "command": "npx",
      "args": [
        "@playwright/mcp@latest",
        "--headless",
        "--browser", "chromium",
        "--isolated",
        "--output-dir", "./test_reports/playwright",
        "--viewport-size", "1440x900"
      ]
    }
  }
}

# Nombre visible de la app (sidebar, pantallas de auth, <title>). Bautiza tu proyecto aqui.
REACT_APP_APP_NAME=MiApp

# URL publica del backend (sin barra final). El frontend le agrega /api a cada llamada.
REACT_APP_BACKEND_URL=http://localhost:8001
